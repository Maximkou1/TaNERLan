"""Data augmentation experiments for the Uzbek NER baseline.

Each experiment (transliteration, external corpora, synthetic augmentation) is
run and evaluated independently, without mixing effects, so its impact on
dev metrics can be attributed on its own.
"""
